class Create<%= @model_name.camelize %> < ActiveRecord::Migration
  def change
    create_table :<%= @model_name %> do |t|
      t.string    :name, null: false
      t.integer
      t.timestamps
    end
  end
end
